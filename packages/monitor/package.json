{
  "name": "@txfence/monitor",
  "version": "0.1.2",
  "license": "MIT",
  "description": "On-chain reconciliation monitor for txfence. Detects unrecorded transactions and chain reorganizations by scanning blocks for known agent addresses.",
  "type": "module",
  "private": false,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "files": [
    "dist",
    "README.md",
    "CHANGELOG.md"
  ],
  "scripts": {
    "build": "tsup",
    "prepublishOnly": "pnpm run build",
    "clean": "rm -rf dist",
    "test": "vitest run"
  },
  "dependencies": {
    "@txfence/core": "0.1.2",
    "@txfence/evm": "0.1.2",
    "viem": "^2.48.8"
  },
  "devDependencies": {
    "vitest": "^4.1.5",
    "tsup": "^8.0.0",
    "@types/node": "^20.0.0"
  }
}
