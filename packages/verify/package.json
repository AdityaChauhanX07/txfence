{
  "name": "@txfence/verify",
  "version": "0.1.2",
  "license": "MIT",
  "description": "Formal policy verification for txfence. Bounded model checking with counterexample generation and adversarial stress testing across six attack vectors.",
  "type": "module",
  "private": false,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "files": [
    "dist",
    "README.md",
    "CHANGELOG.md"
  ],
  "scripts": {
    "build": "tsup",
    "prepublishOnly": "pnpm run build",
    "clean": "rm -rf dist",
    "test": "vitest run"
  },
  "dependencies": {
    "@txfence/core": "0.1.2"
  },
  "devDependencies": {
    "vitest": "^4.1.5",
    "tsup": "^8.0.0",
    "@types/node": "^20.0.0"
  }
}
